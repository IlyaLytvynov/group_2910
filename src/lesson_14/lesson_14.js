import "./lesson_14.scss";
import { slideShow } from "../common/slideshow";
import { analize } from "../common/myCoolMetric";

const point = document.querySelector("#rootSlide");

slideShow(point);
analize();
